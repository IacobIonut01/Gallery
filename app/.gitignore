/build
/release
*.jks

# Prebuilt native libheif/libde265 static libs + headers (large, regenerable).
# Produced by scripts/native/build-heif.sh; not committed to avoid repo bloat.
/src/main/cpp/heif/

# Prebuilt native libjpeg-turbo/libpng/zlib static libs + headers (large, regenerable).
# Produced by scripts/native/build-imgcodec.sh; not committed to avoid repo bloat.
/src/main/cpp/imgcodec/

# Prebuilt native libheif(encoders)/x265/aom/libde265 static libs + headers (large, regenerable).
# Produced by scripts/native/build-heif-encode.sh; not committed to avoid repo bloat.
/src/main/cpp/heifenc/

# Prebuilt native LibRaw/libtiff/libjpeg/zlib static libs + headers (large, regenerable).
# Produced by scripts/native/build-libraw.sh; not committed to avoid repo bloat.
/src/main/cpp/rawcodec/