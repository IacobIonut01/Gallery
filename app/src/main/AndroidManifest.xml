<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:targetSandboxVersion="2"
    tools:ignore="SelectedPhotoAccess, ProtectedPermissions">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required to query Android Advanced Protection Mode (AAPM) state on Android 16+ -->
    <uses-permission android:name="android.permission.QUERY_ADVANCED_PROTECTION_MODE" />

    <!-- Required to access images for SDK 33+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Required to access videos for SDK 33+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Required to access GPS Data of Media -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Required to download Map Preview for Media and FCast media serving -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Required for FCast device discovery and media streaming -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>

    <!-- Remove unwanted permissions from being added -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" tools:node="remove"/>

    <!-- Required to access media for SDK < 33 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Required to modify/delete media on SDK 29 (Android 10) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!--
        (OPTIONAL)
        Requested to bypass another user confirmation requested by
        ScopedStorage when the user wants to trash, delete or add/remove favorites media
    -->
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <!--
        (OPTIONAL)
        Requested to create albums outside Internal Storage/Pictures or Internal Storage/DCIM
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <!-- Foreground service type required for encryption/decryption background tasks on SDK 34+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROCESSING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <queries>
        <intent>
            <action android:name="android.intent.action.EDIT" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.EDIT" />
            <data android:mimeType="video/*" />
        </intent>
    </queries>

    <application
        android:name=".GalleryApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gallery"
        tools:targetApi="33">
        <activity
            android:name=".feature_node.presentation.main.MainActivity"
            android:exported="true"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Gallery.Splash">
        </activity>

        <activity-alias
            android:name=".Launcher_ReFra"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".feature_node.presentation.main.MainActivity"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.APP_GALLERY"/>
                <category android:name="android.intent.category.INFO"/>
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".Launcher_Gallery"
            android:label="@string/app_name_gallery"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".feature_node.presentation.main.MainActivity"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.APP_GALLERY"/>
                <category android:name="android.intent.category.INFO"/>
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".feature_node.presentation.standalone.StandaloneActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/Theme.Gallery">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="com.android.camera.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW_SECURE" />

                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:mimeType="image/*"/>
                <data android:mimeType="video/*"/>

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="com.android.camera.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW_SECURE" />

                <data android:scheme="content"/>
                <data android:scheme="http"/>
                <data android:scheme="file"/>

                <!-- Hacky way to open JPEG-XL files -->
                <data android:mimeType="image/jxl"/>
                <data android:host="*" />

                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.jxl" />
                <data android:pathPattern=".*\\..*\\.jxl" />
                <data android:pathPattern=".*\\..*\\..*\\.jxl" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.jxl" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <data android:scheme="content"/>
                <data android:scheme="file"/>

                <!-- Open APNG files -->
                <data android:mimeType="image/apng"/>
                <data android:host="*" />

                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.apng" />
                <data android:pathPattern=".*\\..*\\.apng" />
                <data android:pathPattern=".*\\..*\\..*\\.apng" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.apng" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature_node.presentation.picker.PickerActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/Theme.Gallery">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.PICK"/>
                <action android:name="android.intent.action.GET_CONTENT"/>

                <data android:mimeType="image/*"/>
                <data android:mimeType="video/*"/>
                <data android:mimeType="vnd.android.cursor.dir/image"/>
                <data android:mimeType="vnd.android.cursor.dir/video"/>

                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.OPENABLE"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".feature_node.presentation.wallpaper.SetWallpaperActivity"
            android:exported="true"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleInstance">
            <intent-filter android:label="@string/set_wallpaper">
                <action android:name="android.intent.action.ATTACH_DATA"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".feature_node.presentation.edit.EditActivity"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:screenOrientation="fullSensor"
            tools:ignore="DiscouragedApi"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.EDIT"/>

                <category android:name="android.intent.category.DEFAULT"/>

                <data android:mimeType="image/*"/>
                <data android:mimeType="vnd.android.cursor.dir/image"/>
            </intent-filter>
        </activity>

        <!-- Widget Configuration Activity -->
        <activity
            android:name=".feature_node.presentation.widget.WidgetConfigActivity"
            android:exported="true"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:theme="@style/Theme.Gallery">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Single Media Widget -->
        <receiver
            android:name=".feature_node.presentation.widget.SingleMediaWidgetReceiver"
            android:label="@string/widget_single_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/single_media_widget_info" />
        </receiver>

        <!-- Grid Media Widget -->
        <receiver
            android:name=".feature_node.presentation.widget.GridMediaWidgetReceiver"
            android:label="@string/widget_grid_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/grid_media_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${appProvider}"
            android:enabled="true"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove">
            <meta-data
                android:name="com.dot.gallery.ui.ComposeInitializer"
                android:value="androidx.startup"
                />
        </provider>

        <!-- Isolated-process service for sandboxed metadata parsing.
             Runs under a fake UID with no permissions, no app data, and no network.
             Parser bugs in metadata-extractor or MediaMetadataRetriever cannot
             reach the user's media library or network from this process. -->
        <service
            android:name=".core.sandbox.IsolatedMetadataService"
            android:exported="false"
            android:isolatedProcess="true" />

        <!-- Isolated-process service for sandboxed image decoding (HEIF/AVIF/JXL).
             Codec bugs in HeifCoder or JxlCoder cannot reach the main app process.
             Data is exchanged via SharedMemory for zero-copy IPC. -->
        <service
            android:name=".core.sandbox.IsolatedDecoderService"
            android:exported="false"
            android:isolatedProcess="true" />

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            tools:node="merge"
            android:foregroundServiceType="dataSync|mediaProcessing" />

    </application>

</manifest>